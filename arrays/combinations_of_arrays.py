"""
combinations_of_array
"""

def combinations_of_array(lista):
	if not lista or len(lista)==0:
		return -1
	elements = 1
	# for ele in lista:
	# 	elements *= len(ele)
	# return elements
	for ele in lista:
		for 
		elements *= len(ele)
	return elements

if __name__ == "__main__":
	 lista = [
	 	['a', 'b','c'],
	 	['d', 'e','f', 'g'],
	 	['h', 'i','j', 'k', 'l']]
	 print combinations_of_array(lista)

	 shirts = [
	 	["colour", "red", "blue", "green", "yellow"],
		["cloth", "cotton", "poly", "silk"],
		["type", "full", "half"]]